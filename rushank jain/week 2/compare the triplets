vector<int> compareTriplets(vector<int> a, vector<int> b) {
int scoreA = 0;
int scoreB = 0;
    for (int i=0;i<3;i++) {
        if (a[i] > b[i]) {
            scoreA = scoreA + 1;
        } else if (a[i] < b[i]) {
            scoreB = scoreB + 1;
        }
    }
return int[]arr{scoreA, scoreB} ;
